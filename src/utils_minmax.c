#include "miniRT.h"

double	min(double num1, double num2)
{
	if (num1 < num2)
		return (num1);
	return (num2);
}

double	max(double num1, double num2)
{
	if (num1 < num2)
		return (num1);
	return (num2);
}
